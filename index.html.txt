
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<title>Funnlorn</title>

<style>
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #5f2cff, #9b4dff);
    color: white;
    text-align: center;
    overflow-x: hidden;
    transition: background 0.5s;
}

/* Анимация появления */
.fade {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeIn 1s forwards;
}

@keyframes fadeIn {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

header {
    padding: 40px;
}

.avatar {
    width: 160px;
    height: 160px;
    border-radius: 50%;
    border: 4px solid white;
    animation: pulse 2s infinite;
}

/* Пульсация */
@keyframes pulse {
    0% {transform: scale(1);}
    50% {transform: scale(1.05);}
    100% {transform: scale(1);}
}

section {
    max-width: 700px;
    margin: auto;
    padding: 20px;
}

.card {
    background: rgba(255,255,255,0.1);
    padding: 20px;
    margin: 15px 0;
    border-radius: 15px;
    transition: 0.3s;
}

.card:hover {
    transform: scale(1.05);
}

/* Фото */
.gallery img {
    width: 100%;
    border-radius: 10px;
    margin-top: 10px;
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 1s forwards;
    transition: 0.3s;
}

.gallery img:hover {
    transform: scale(1.05);
}

/* Соцсети */
.socials a {
    display: inline-block;
    margin: 10px;
    padding: 10px 20px;
    background: white;
    color: #5f2cff;
    border-radius: 10px;
    text-decoration: none;
    font-weight: bold;
    transition: 0.3s;
}

.socials a:hover {
    background: #ddd;
    transform: scale(1.1);
}

/* Донат кнопка */
.donate {
    display: inline-block;
    margin-top: 20px;
    padding: 15px 30px;
    background: linear-gradient(45deg, #ff3c7f, #ff75a0);
    color: white;
    border-radius: 15px;
    font-size: 18px;
    text-decoration: none;
    font-weight: bold;
    animation: glow 1.5s infinite alternate;
    transition: 0.3s;
}

.donate:hover {
    transform: scale(1.1) translateY(-3px);
}

@keyframes glow {
    from {box-shadow: 0 0 10px #ff3c7f;}
    to {box-shadow: 0 0 25px #ff3c7f;}
}

/* Кнопка */
button {
    margin-top: 15px;
    padding: 10px 20px;
    border: none;
    border-radius: 10px;
    background: white;
    color: #5f2cff;
    cursor: pointer;
    transition: 0.3s;
}

button:hover {
    transform: scale(1.1);
}

/* Мобильная адаптация */
@media screen and (max-width: 600px) {
    .avatar { width: 120px; height: 120px; }
    .card { padding: 15px; margin: 10px; }
    .socials a, .donate, button { padding: 10px 15px; font-size: 16px; }
}
</style>
</head>

<body>

<header class="fade">
    <img src="avatar.jpg" class="avatar">
    <h1 id="name">Funnlorn</h1>
    <p id="bio">Стример | Контент-креатор | Будущий бренд энергетиков</p>
</header>

<section>

<div class="card fade">
    <h2>📖 Биография</h2>
    <p>
        Николай (funnlorn) — стример и контент-креатор.
        Родился в Хабаровске, живёт в Краснодаре.
        Работает на Ozon и активно развивает стриминг.
        Основной фокус — live-контент и взаимодействие с аудиторией.
        Цель — выйти на высокий уровень и создать свой бренд энергетиков.
    </p>
</div>

<div class="card gallery fade">
    <h2>📸 Мои фото</h2>
    <div id="galleryContainer"></div>
</div>

<div class="card socials fade">
    <h2>🌐 Соцсети</h2>
    <a href="https://t.me/forever_gtc" target="_blank">Telegram</a>
    <a href="https://youtube.com/@funnlorn1" target="_blank">YouTube</a>
    <a href="https://www.twitch.tv/funnlorn1" target="_blank">Twitch</a>

    <br>

    <!-- Донат -->
    <a href="https://www.donationalerts.com/r/funnlorn11" target="_blank" class="donate">
        💸 Поддержать донатом
    </a>
</div>

<button onclick="secret()">Press 😏</button>

</section>

<script>
// Кнопка секрет
function secret() {
    document.getElementById("name").innerText = "Funnlorn TOP 😎";
    document.getElementById("bio").innerText = "Ты уже почти топ стример 🔥";
    document.body.style.background = "linear-gradient(135deg, #ff3c7f, #ff75a0)";
}

// Динамическая галерея
const photos = ["photo1.jpg", "photo2.jpg", "photo3.jpg"]; // <-- добавляй сюда свои фото
const gallery = document.getElementById("galleryContainer");

photos.forEach((src, index) => {
    const img = document.createElement("img");
    img.src = src;
    img.alt = `Фото ${index + 1}`;
    img.style.animationDelay = `${0.3 + index*0.3}s`; // плавная задержка
    gallery.appendChild(img);
});
</script>

</body>
</html>